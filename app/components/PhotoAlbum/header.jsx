var React = require('react');

var Header = React.createClass({

  render: function() {
    return (
      <header>
        <h1>My Photo Album</h1>
      </header>
    );
  }
});

export default Header;
